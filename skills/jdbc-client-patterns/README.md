---
name: jdbc-client-patterns
description: Spring JdbcClient patterns for this project. Named params, row mappers, batch ops, transactions. No JPA/ORM.
---

Spring `JdbcClient` is the only persistence abstraction in this project. No JPA, no Hibernate, no Spring Data repositories.

See `SKILL.md` for complete patterns.
